version: '3.8'
services:
  web:
    build: .
    container_name: inspection-web
    ports: ["8000:8000"]
    volumes: [".//app/data", "./reports:/app/reports", "./inspection.db:/app/inspection.db"]
    restart: unless-stopped