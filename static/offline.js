const DB='InspDB', STORE='pending'; let db;
function initDB(){return new Promise((res,rej)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=e=>{db=e.target.result;db.createObjectStore(STORE,{keyPath:'id',autoIncrement:true})};r.onsuccess=e=>{db=e.target.result;res()};r.onerror=e=>rej(e)})}
function save(d){return new Promise((res,rej)=>{const t=db.transaction(STORE,'readwrite');t.objectStore(STORE).add(d);t.oncomplete=res;t.onerror=rej})}
function getPending(){return new Promise((res,rej)=>{const t=db.transaction(STORE,'readonly');const r=t.objectStore(STORE).getAll();r.onsuccess=()=>res(r.result||[]);r.onerror=rej})}
function del(id){return new Promise((res,rej)=>{const t=db.transaction(STORE,'readwrite');t.objectStore(STORE).delete(id);t.oncomplete=res;t.onerror=rej})}
async function sync(){if(!navigator.onLine)return;const p=await getPending();for(const i of p){try{const fd=new FormData();Object.keys(i).forEach(k=>fd.append(k,i[k]));const r=await fetch(`/submit/${i.code}`,{method:'POST',body:fd,redirect:'manual'});if(r.ok||r.status===302){await del(i.id);upd('✅ رفعت الإجابات المخزنة')}}catch(e){break}}}
function upd(m){const e=document.getElementById('sync-status');if(e){e.textContent=m;e.classList.remove('hidden')}}
document.addEventListener('DOMContentLoaded',async()=>{await initDB();upd(navigator.onLine?'🌐 متصل':'📴 أوفلاين - يحفظ محلياً');window.addEventListener('online',()=>{upd('🔄 مزامنة...');sync()});window.addEventListener('offline',()=>upd('📴 انقطع - يحفظ محلياً'));setInterval(sync,60000);sync()});
window.handleOfflineSubmit=async function(form,code){event.preventDefault();const btn=form.querySelector('button[type=submit]');btn.disabled=true;btn.textContent='جاري...';
const data={code,...Object.fromEntries(new FormData(form))};data.answers_json=JSON.stringify(Object.fromEntries(new FormData(form)));
try{if(navigator.onLine){const fd=new FormData(form);await fetch(`/submit/${code}`,{method:'POST',body:fd,redirect:'manual'})}else{await save(data);upd('💾 حفظ محلياً')}location.href='/inspect/success'}catch(e){await save(data);upd('💾 حفظ محلياً');setTimeout(()=>location.href='/inspect/success',1000)}}